<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:fontify="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout_main"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/navigation_bar_height"
        android:layout_alignParentLeft="true"
        android:background="?android:attr/activatedBackgroundIndicator"
        android:gravity="center_vertical" >

        <ImageView
            android:id="@+id/drawer_icon"
            android:layout_width="@dimen/drawer_icon_size"
            android:layout_height="@dimen/drawer_icon_size"
            android:layout_marginRight="@dimen/padding_gap"
            android:contentDescription="contentDescription"
            android:paddingLeft="@dimen/padding_gap"
            android:src="@drawable/flash_app_logo"
            android:layout_gravity="center_vertical"/>

        <TextView
            android:id="@+id/drawer_itemName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/navigation_list_item_text"
            android:textSize="@dimen/h6_text_size"
            android:text="Settings"
            android:layout_gravity="center"/>
    </LinearLayout>

</RelativeLayout>